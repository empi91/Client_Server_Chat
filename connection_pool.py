"""Connection Pool module for concurrent connections to database

This module provides a connection pool, which allocated several open connections to database
at the start, disposes them as needed to required functions and, if necessary, opens and closes additional ones, as per demand
"""

class ConnectionPool:

    def __init__(self):

        pass


    
